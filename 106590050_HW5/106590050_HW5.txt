1.Tests that are compiled and run entirely on your local machine with the Java Virtual Machine (JVM).

2.com.example.android.SimpleCalcTest (test)

3.@Test

4.Copy the image files into your project's drawable folder.

5.Add the android:onClick attribute to the ImageView in the layout and use it to call the click handler in the Activity.

6.The click handler method must be public, return void, and define a View as its only parameter.